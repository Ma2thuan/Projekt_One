@model Projekt_1.Model.WithdrawalSlip

@{
    ViewBag.Title = "Delete Withdrawal Slip";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<h2 class="mb-4 text-danger">Delete Withdrawal Slip</h2>

<div class="card shadow mb-4">
    <div class="card-header bg-danger text-white">
        <h4 class="mb-0">Confirm Deletion</h4>
    </div>
    <div class="card-body">
        <p class="mb-3">Are you sure you want to delete this withdrawal slip? This action cannot be undone.</p>
        <dl class="row">
            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.WithdrawalAmount)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.WithdrawalAmount)</dd>

            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.WithdrawalDate)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.WithdrawalDate)</dd>

            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.passbook.SavingsBookID)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.passbook.SavingsBookID)</dd>

            <dt class="col-sm-4">@Html.DisplayNameFor(model => model.user.user_address)</dt>
            <dd class="col-sm-8">@Html.DisplayFor(model => model.user.user_address)</dd>
        </dl>
    </div>
    <div class="card-footer d-flex justify-content-start gap-2">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <button type="submit" class="btn btn-danger">Delete</button>
        }
        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-secondary" })
    </div>
</div>
